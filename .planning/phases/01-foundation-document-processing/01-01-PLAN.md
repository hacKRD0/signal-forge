---
phase: 01-foundation-document-processing
type: execute
---

<objective>
Set up Python project foundation with Google ADK and core dependencies.

Purpose: Establish the base project structure and install Google ADK (Agent Development Kit) to enable AI agent development with built-in web search capabilities.
Output: Runnable Python project with Google ADK installed, project structure created, and basic configuration files.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create project structure and virtual environment</name>
  <files>.gitignore, README.md, requirements.txt</files>
  <action>
Create Python project structure:
- Root directory structure: src/, data/, logs/, tests/
- Create .gitignore with Python standard ignores (venv/, __pycache__/, *.pyc, .env, logs/, *.log)
- Create empty README.md placeholder
- Create empty requirements.txt
- Create Python virtual environment: python -m venv venv
- Verify virtual environment activation works
  </action>
  <verify>
- Directory structure exists: ls src/ data/ logs/ tests/
- Virtual environment exists: ls venv/
- .gitignore contains Python ignores
  </verify>
  <done>Project directories created, .gitignore configured, virtual environment ready</done>
</task>

<task type="auto">
  <name>Task 2: Install Google ADK and core dependencies</name>
  <files>requirements.txt</files>
  <action>
Add to requirements.txt:
- google-genai (Google ADK - Agent Development Kit)
- python-dotenv (for environment variables)
- Add versions for stability (use latest stable versions)

Install dependencies:
- Activate virtual environment
- Run: pip install -r requirements.txt
- Verify Google ADK installation: python -c "import google.genai; print(google.genai.__version__)"
  </action>
  <verify>
- requirements.txt contains google-genai, python-dotenv with versions
- pip list shows installed packages
- Import test succeeds without errors
  </verify>
  <done>Google ADK and core dependencies installed and verified</done>
</task>

<task type="auto">
  <name>Task 3: Create configuration and environment setup</name>
  <files>.env.example, src/config.py</files>
  <action>
Create .env.example with:
- GOOGLE_API_KEY=your_api_key_here (placeholder for Google ADK API key)
- LOG_LEVEL=INFO
- DATA_DIR=data

Create src/config.py:
- Load environment variables using python-dotenv
- Define configuration class with API key, log level, data directory paths
- Add validation to ensure required environment variables are set
- Include helper function to get config instance

Do NOT create actual .env file (user will create from .env.example with their own API key).
  </action>
  <verify>
- .env.example exists with all required variables
- src/config.py imports successfully: python -c "from src.config import *"
- Configuration structure is clear and documented
  </verify>
  <done>.env.example created, src/config.py implemented with environment variable loading</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Project structure exists (src/, data/, logs/, tests/)
- [ ] Virtual environment created and activatable
- [ ] requirements.txt contains Google ADK dependencies
- [ ] Dependencies installed: pip list shows google-genai
- [ ] .env.example and src/config.py exist
- [ ] No errors when importing src.config
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Python project foundation ready for document processing implementation
- Google ADK installed and importable
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-document-processing/01-01-SUMMARY.md`:

# Phase 1 Plan 1: Project Foundation Summary

**Python project initialized with Google ADK and core dependencies**

## Accomplishments
- Created project structure (src/, data/, logs/, tests/)
- Installed Google ADK and core dependencies
- Configured environment variable management

## Files Created/Modified
- `.gitignore` - Python standard ignores
- `requirements.txt` - Google ADK and dependencies
- `.env.example` - Environment variable template
- `src/config.py` - Configuration management

## Decisions Made
None

## Issues Encountered
[Document any issues, or "None"]

## Next Step
Ready for 01-02-PLAN.md (Document parsers: DOCX, PDF)
</output>
